//You are given a C program that initializes an array of 5 integers 
//and needs to print the middle element. 
//Complete the code so that it correctly prints the middle value
#include <stdio.h>

int main() {
    // An array with 5 elements
    int numbers[5] = {15, 25, 35, 45, 55};

    // Print the middle element 
    printf("Middle element is: %d\n", numbers[2]);

    return 0;
}
